ERROR/BUG CORRECTION AND FEATURE INTEGRATION/REFINEMENT TASKS:
1. Horizontal scroll addition and proper responsive scaling+wrap text toggle for the export panel popup box and all of its sub-elements.
2. Improvements to and further automated of the Research Activity + Sources tab contents export: 
	1. Robust, accurate, and selective extraction of the true real number of sources scanned within the sources scanned header (currently higher than reported in the actual interactive browser session and originating html) and corresponding bullet numbering identical to the true web / html report contents
	2. Formatting of sources scanned sub content/nested bullet entry characters to prevent numbered bullet conflicts/formatting disruption without having to manually click each tab and reexport two separate times (instead automated clicking, loading and export of both at once), despite both boxes being checked.
	3. Proper extraction and formatting of not only each research activity bold summary header, but also the proper extraction and formatting of their respective sub entry/nested numbered bullet children thought contents.
	4. Improve the reliability of the export regardless of where it is called from (main chat or deep research report maximized, or deep research report maximized with either of the right panel tabs opened).
3. Proper implementation of the sub element selection within the maximized panel.
4. Prevent/remove multiple panels spawning (duplicate/redundant panels).
5. Fix the hooking/interception of the esc key while user script is activated to actually exit it rather than just partially dismiss it and/or minimizing the deep research report iframe.
6. Improve the reliability, precision and consistency of the dom/panel targeting.
7. Improve the universal website markdown extraction/conversion logic to avoid interfering and failing to extract when selecting non-Deep Research elements/content on other websites and within non-Deep Research ChatGPT content.
	1. Remove and prevent the interference that the current script sometimes causes with the loading of certain pages, leaving them entirely blank indefinitely or blocking the loading of their respective content when the script is passively enabled (e.g., google.com searches, signing in with google to certain websites etc.)
	2. Ensure that the universal extractor button utilizes an enhanced robust extraction mechanism and has a more focused panel than the deep research specific extractor such that extraction and conversion from any site functions as normal, unless the deep research export button is used.
8. Ensure that the Google Gemini Deep Research extraction mechanism is fully compatible with the current web/html formatting of it's content (i.e. Report Contents; Sources used in the report; Sources read but not used in the report; Thoughts) and outputs the same standardized sources and thoughts formatting with favicons removed etc. that the ChatGPT Deep Research extractor/convertor does (i.e. ordered/numbered bulleted nested lists, bold text where appropriate etc. to perfectly match the content within the live html web version).