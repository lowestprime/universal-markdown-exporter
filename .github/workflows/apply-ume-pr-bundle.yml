name: Disabled temporary UME bundle workflow

on:
  workflow_dispatch:

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo 'This temporary workflow is intentionally disabled.'
